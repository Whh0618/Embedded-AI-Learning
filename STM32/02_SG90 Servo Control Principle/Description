1. SG90 舵机控制原理：
舵机控制需要一个 20ms 的PWM信号，PWM 信号由高低电平组成，舵机内部有识别高电平时间的装置，具体时间的高电平对应角度如下：
0.5ms------0°    1.0ms-------45°     1.5ms------90°     2.0ms--------135°    2.5ms----------180°

2. 如何通过单片机的时钟生成一个 20 ms的PWM信号：
由定时器通过时钟节拍生成，本文时钟树中的TIM定时器的频率为36MHz，需要设置定时器中的相关参数生成 20ms 的PWM信号。如：分频系数（PSC），计数周期（ARR：自动重装载寄存器实现），
以及CCR（捕获/比较寄存器）。
首先，用户需要定义TIM每计数一次所需时间(1us)，1us也就是1MHz，PSC(分频系数)的计算公式为：计数频率/每次 = TIM/1+(PSC)，因TIM为36MHz，所以PSC为35；
因为1us计数一次，那么20ms计数20000次，所以ARR就从0计数到19999。
CCR控制20msPWM信号中高电平的时间，因此当CCR为500时，ARR在计数到500之前均保持输出电平为高电平，超过500之后均为低电平，而500个计数又是0.5ms，所以舵机转动角度0°。

3. 在代码中，通过HAL库函数变换CCR(高电平占空比)来控制舵机角度。
